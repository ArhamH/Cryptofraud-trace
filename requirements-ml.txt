# Optional — NOT required for the base app (gnn_risk_model.py falls back
# to a zero-dependency heuristic scorer without these). Install only when
# training or running a real GraphSAGE/EvolveGCN checkpoint:
#
#   pip install -r requirements-ml.txt
#
# Deliberately kept out of requirements.txt: these are heavy, often
# GPU-oriented packages that will break a free-tier Streamlit Community
# Cloud deploy if installed unconditionally.
torch>=2.1.0
torch-geometric>=2.4.0
torch-geometric-temporal>=0.54.0   # EvolveGCN, for the temporal upgrade path — see train_graphsage.py
